import React from 'react';
import {VStack} from "@chakra-ui/react";

const SideBar = () => {
  return (
    <VStack bgColor={'var(--side-bar-color)'} w={'100%'} h={'100%'}>
      asd
    </VStack>
  );
};

export default SideBar;
